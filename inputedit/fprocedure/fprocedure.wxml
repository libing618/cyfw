<import src="../../libs/wepage.wxml" />
<view class="page" id="approval-{{pNo}}">
  <template is="p_navBar__back" data="{{...pw,navBarTitle}}"/>
  <template is="p_{{sPages[sPages.length-1].pageName}}" data="{{...sPages[sPages.length-1],showModalBox,animationData,vData,pageData,reqData}}"/>
</view>
